name: ${name}
version: ${version}

mains:
  papermc: "${group}.papermc.PlayerTools"
  spigotmc: "${group}.spigotmc.PlayerTools"
  foliamc: "${group}.foliamc.PlayerTools"

git:
  provider: github
  owner: MCPaimon
  repository: extension-playertools
